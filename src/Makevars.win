
## OLD
#PKG_CPPFLAGS = -I${R_HOME}/library/RcppEigen/include \
#-I${R_HOME}/library/Rcpp/include -I. -DNDEBUG

## old


PKG_LIBS = $(shell "${R_HOME}/bin${R_ARCH_BIN}/Rscript.exe" --vanilla -e "Rcpp:::LdFlags()") 

PKG_CPPFLAGS = -I. -DNDEBUG

